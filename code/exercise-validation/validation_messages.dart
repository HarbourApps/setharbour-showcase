/// User-facing validation copy for numeric workout inputs.
///
/// The message below is an intentional, finished piece of SetHarbour brand
/// personality — it pairs a hard validation rule (three digits maximum) with a
/// light, friendly tone. It is deliberately preserved exactly as it appears in
/// the production application and the supplied screenshots.
///
/// Do NOT reword, formalise or "clean up" this string. It is product copy, not
/// a placeholder. The rule that produces it lives separately in
/// [NumericInputValidator] so the wording and the logic can evolve
/// independently.
const String kSupermanValidationMessage =
    'This app is not made for Superman — only 3 digits maximum 🦸';
